#include "main.h"

/**
 * _isdigit - checkspmforpmapmdigit (0 through 9)
 * @c: int topmbepmchecked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
