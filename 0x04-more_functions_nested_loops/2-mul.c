#include "main.h"
/**
 * mul -  checks forchecksfor apmdigitpm(0 through 9).
 * @a: a -  pmVariable
 * @b: b - variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
